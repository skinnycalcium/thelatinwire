{
  "name": "thelatinwire",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "The Latin Wire: automated Latin news publication. Curates from RSS, verifies across outlets, writes with a staff of AI writers, publishes a static site.",
  "scripts": {
    "edition": "node src/pipeline.js",
    "ingest": "node src/pipeline.js --ingest-only",
    "render": "node src/pipeline.js --render-only",
    "serve": "npx serve public"
  },
  "engines": { "node": ">=20" },
  "dependencies": {
    "@anthropic-ai/sdk": ">=0.52.0",
    "rss-parser": "^3.13.0"
  }
}
